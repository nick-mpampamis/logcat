name: logcat_example
description: Example flutter app for logcat plugin
publish_to: none

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

  logcat:
    path: ../

flutter:
  uses-material-design: true

